<?xml version="1.0" encoding="UTF-8" ?><text src="dbbgn 0.000|">Также отправляются аутентификационные данные. Обычно сервер посылает аутентификационную информацию в форме сертификата (встроенную в сертификат) X.509 (SSL), но протокол поддерживает и другие методы.
Далее устанавливается идентификатор сессии (ID), таким образом, при необходимости сессия может быть перезапущена, и происходят переговоры о наборе шифров. </text>