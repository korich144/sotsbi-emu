<?xml version="1.0" encoding="UTF-8" ?><text src="dbbgn 0.000|">Часто, для обеспечения стабильности работы сети, используют избыточные линии связи (redundant links). При этом, если  между коммутаторами по какой-то причине не работает один link, подключается резервное соединение.
Рассмотрим ситуацию, когда два коммутатора соединены двумя сетевыми кабелями (например, что у них соединены порты fa0/1 и fa0/24).
К коммутатору S1 подключен компьютер, который посылает в сеть широковещательный кадр (например, ARP-запрос). </text>