<?xml version="1.0" encoding="UTF-8" ?><text>При перехвате TCP-сегмента обычно на основе транспортного порта указывается протокол более высокого уровня. В данном случае сообщение HTTPS размером в 66 байт передается поверх TCP. Можно сосчитать, что служебный заголовок составл¤ет 32 байта, что и указано в поле Header lenght, под которое отводится 4 бита. Поля транспортных портов источника и получателя составляют каждое по 2 байта и имеют значение 7939 и 443 для HTTPS. 4 байта отведено на порядковый номер. Номер подтверждения так же занимает 4 байта, но в данном случае имеет нулевое значение. Поле control bits занимает 6 бит, и еще 2 бита используются для флагов из зарезервированного поля. Поле окно занимает 2 байта. И еще 2 байта отведено на проверочную кодовую комбинацию. Кроме стандартных 20 байт служебного заголовка TCP, 12 байт используется под необязательные заголовки: максимального размера сегмента, несколько No Operation (NOP) для запроса отклика, машстабирование поля окна в значении 8 (в 256 раз, вплоть до 1 Гигабайта) и разрешение выборочного подтверждения (SACK Permitted).</text>