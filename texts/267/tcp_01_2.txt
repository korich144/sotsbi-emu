<?xml version="1.0" encoding="UTF-8" ?><text src="dbbgn 0.000|" uts="1493742881">Особенностью протокола TCP является наличие гарантии на доставку данных, что позволяет ему работать без потерь пакетов, но ценой увеличения задержки пакетов. По этой причине он получил применение для передачи данных файлового трафика (с применением протоколов FTP и HTTP). Протокол TCP применяется для передачи данных с установлением логического соединения и гарантии доставки, а так же для передачи данных чувствительных к потерям. Целесообразно применять протокол TCP для данных неучувствительных к задержкам пакетов (например, при обмене файлами.В XXI веке получил распространение метод TCP-туннелирования, когда трафик приложений между сетями или устройствами помещается в TCP сегменты для надежного прохождения через сеть. Например, VPN circuit proxy, который ретранслирует трафик из защищенной сети в общедоступную сеть для каждого сокета в отдельности (TCP-соединения, т.е. комбинации IP-адреса и конкретного порта). Другим примером является протокол SOCKS 5. Клиентский компьютер по протоколу SOCKS 5 запрашивает у прокси TCP-соединение к серверу в защищенной сети, и получает IP и TCP-порт для такого соединения. Прокси получает и передает сегменты TCP на целевой сервер.</text>